## 2026-03-09

- 执行 `news-analysis`，扫描并处理了 `input/data/articles/` 下全部待分析新闻；运行期间新增 1 条，因此最终共处理 32 条。
- 32 条新闻均已生成三视角报告并保存到 `memory/stock-analysis/2026-03-09/`，原始新闻均已归档到 `input/data/archived/2026-03-09/`。
- 飞书发送结果：
  - `莫岳恒`：前 10 条新闻的 5 段消息发送成功；后续发送失败。
  - `黄旭`：始终失败，飞书接口返回 `230013 Bot has NO availability to this user`。
- 运行中发现 `.env` 中飞书配置发生变化，最终值为：
  - `FEISHU_APP_ID=cli_a926ed456638dcd4`
  - `FEISHU_APP_SECRET=tz6PD8Lx6Q1k76HqvOVcZgkj3U0BlEMo`
  - `FEISHU_USER_ALIAS_JSON` 仍是坏 JSON，`黄旭` 这一项缺少引号。
- `莫岳恒` 后续发送失败的错误先后出现：
  - `10014 app secret invalid`
  - `99991672` 缺少 `im:message:send` / `im:message:send_as_bot` 权限
- 下次如果继续使用飞书发送，先核对 `.env` 当前 app 是否正确、是否具备消息发送权限，再重跑未发送部分。
- `scripts/feishu_ws_probe.py` 已从 20 秒临时探针改成飞书长链接客户端：默认常驻直到收到 `SIGINT`/`SIGTERM`，保留 `--duration-seconds` 作为限时探测模式。
- 根目录 `pyproject.toml` 已显式加入 `lark-oapi>=1.5.3`；脚本缺少 SDK 时会输出 JSON 错误而不是导入时报错。
